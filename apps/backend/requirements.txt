# FastAPI: 파이썬 기반의 고성능 웹 백엔드 프레임워크
fastapi>=0.110.0

# Uvicorn: FastAPI 비동기 서버를 실행해주는 웹 서버(ASGI) 엔진
uvicorn[standard]>=0.28.0

# PyMongo: 파이썬 프로그램과 MongoDB(Atlas)를 연결 및 통신해주는 공식 드라이버
pymongo>=4.13.0

# Pydantic: API 요청/응답 데이터의 타입을 검증하고 스키마를 정의하는 라이브러리
pydantic>=2.6.0

# python-dotenv: .env 파일에 작성된 환경변수를 읽어와 파이썬 코드에서 사용할 수 있게 해주는 라이브러리
python-dotenv>=1.0.0

# HTTPX: FastAPI TestClient 및 비동기 HTTP 요청 지원
httpx>=0.27.0

# Pytest: 단위 및 통합 테스트 실행 프레임워크
pytest>=8.0.0