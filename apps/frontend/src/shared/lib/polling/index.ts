export {
  getPollingInterval,
  POLLING_INTERVAL_MS,
  POLLING_TIMEOUT_MS,
  usePollingWindow,
} from "./polling";
export type { PollingInterval, PollingIntervalOptions, PollingWindow } from "./polling";
